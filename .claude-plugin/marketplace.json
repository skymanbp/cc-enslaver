{
  "name": "cc-enslaver",
  "owner": {
    "name": "skymanbp"
  },
  "metadata": {
    "description": "Single-plugin marketplace exposing the cc-enslaver discipline plugin.",
    "version": "0.25.0"
  },
  "plugins": [
    {
      "name": "cc-enslaver",
      "source": "./",
      "description": "A Claude Code plugin and LLM-agnostic rule pack that enforces systematic thinking, verified citations, and root-cause analysis — eliminating reactive, lazy AI behavior. v0.25 is a second-round audit release: multi-lens adversarial review of the v0.24 tree, every finding independently probe-verified, red-first regressions (323 → 346, 32 of them failing on the pre-fix tree), 12 confirmed defects fixed, zero new features. HIGH: a successful register_read invocation short-circuited the ENTIRE bypass catalog, so `register_read.py --file F --hash H && git push --force` was ALLOWED (deny checks now run first, and a command that will be denied no longer mutates session state); rule 09's flagship try/except-pass detector was defeated by any trailing comment on the `pass` line — which also made its own why-comment escape hatch unreachable — and only ever inspected the first `except` clause, hiding the canonical narrow-handler-then-catch-all shape; reading a path BEFORE it existed granted permanent blind-edit authorization once something created it; an unreadable-but-present state file turned into a false read-before-edit DENY while stderr announced 'failing open'; layer (g)'s Chinese claim regex had no first-person anchor, so 'v0.23 修改了 X' was parsed as a self-claim and truthful third-party attributions were BLOCKED (CJK negation was invisible too); a mis-encoded edicts.toml raised UnicodeDecodeError past every downstream check and silently switched OFF read-before-edit for the whole session. MED: `--file=` spelling and unquoted Windows backslash paths now register (shlex posix mode); force-push detection scoped to the git push sub-command (`rm -f x && git push` no longer false-denies, `git push -fu` no longer escapes); quoted-key secrets (`\"api_key\": \"…\"`, the most common committed-credential shape) now denied; UTF-8 BOM tolerated in both TOML configs via a shared hardened reader (lib/tomlio.py); nested code fences no longer close their parent in the TL;DR and i18n scanners; manage_edicts no longer corrupts the file when re-emitting a multi-line edict. Five of twelve hook scripts were self-locked — unrewritable by any agent running this plugin — now pinned by a tree-wide test. Prior: v0.24 health audit (10 fixes, 3 HIGH); v0.23 rule 12 repo-wide sync + 160-char TL;DR cap; v0.22 rules 10 + 11 content detectors; v0.22.2 the version-drift gate pinning this very file to plugin.json. English is the i18n skeleton and source of truth (rules/ + prompts/ at the root, default injection); Chinese is a first-class translation (rules/zh/ + prompts/zh/, CC_ENSLAVER_LANG=zh), with drift CI-blocked by i18n_check.py + test_i18n_sync.py.",
      "version": "0.25.0",
      "author": {
        "name": "skymanbp"
      },
      "homepage": "https://github.com/skymanbp/cc-enslaver",
      "repository": "https://github.com/skymanbp/cc-enslaver",
      "license": "MIT",
      "keywords": [
        "cc-enslaver",
        "discipline",
        "systematic-thinking",
        "verification",
        "root-cause",
        "rules",
        "hooks",
        "code-quality",
        "llm-agnostic"
      ],
      "category": "code-quality",
      "tags": [
        "claude-code",
        "plugin",
        "discipline",
        "verification"
      ]
    }
  ]
}
